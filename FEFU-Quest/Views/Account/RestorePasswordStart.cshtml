@model RestorePasswordStartViewModel
@{
}

<form asp-action="RestorePasswordStart" asp-antiforgery="true" class="" id="form">
    <p>Введите почту а мы вам на email отправим ссылку востановления)</p>
    <div class="link_line">
        <p>Email:</p>
        <input type="email" placeholder="email" class="input" asp-for="Email" id="Email" />
    </div>
    <div class="link">
        <button name="get_log" class="btn_log" style="width:auto;" id="regIn">Ввести</button>
    </div>
</form>